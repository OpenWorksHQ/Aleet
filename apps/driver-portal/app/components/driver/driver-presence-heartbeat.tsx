"use client";

/**
 * Presence is socket-driven (see lib/socket.ts).
 * This component is kept as a mount point for future HTTP fallbacks only.
 */
export function DriverPresenceHeartbeat() {
  return null;
}
