import { MembershipsPanel } from "@/app/components/admin/memberships/memberships-panel";

export const metadata = {
  title: "Memberships — Aleet Admin",
};

export default function PlatformMembershipsPage() {
  return <MembershipsPanel />;
}
