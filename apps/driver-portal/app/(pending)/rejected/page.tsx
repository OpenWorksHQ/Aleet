import { RejectedPage } from "@/app/components/driver/pending/rejected-page";

export const metadata = {
    title: "Application Rejected — Aleet",
};

export default function Rejected() {
    return <RejectedPage />;
}
