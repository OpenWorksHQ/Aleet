import { PendingReviewPage } from "@/app/components/driver/pending/pending-review-page";

export const metadata = {
    title: "Account Under Review — Aleet",
};

export default function PendingPage() {
    return <PendingReviewPage />;
}
