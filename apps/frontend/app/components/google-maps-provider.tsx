"use client";

/** Maps API calls run through the backend — no browser Google Maps SDK required. */
export function GoogleMapsProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
