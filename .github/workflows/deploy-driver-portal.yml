name: Deploy Driver Portal

on:
  push:
    branches:
      - main
      - master
    paths:
      - "apps/driver-portal/**"
      - ".github/workflows/deploy-driver-portal.yml"

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: apps/driver-portal/package-lock.json

      - name: Generate .env.local
        run: |
          cat > $GITHUB_WORKSPACE/apps/driver-portal/.env.local << 'EOF'
          NEXT_PUBLIC_API_URL=${{ vars.NEXT_PUBLIC_DRIVER_API_URL }}
          NEXT_PUBLIC_SOCKET_URL=${{ vars.NEXT_PUBLIC_DRIVER_SOCKET_URL }}
          NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=${{ secrets.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY }}
          NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=${{ secrets.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY }}
          EOF

      - name: Install dependencies
        run: npm ci
        working-directory: apps/driver-portal

      - name: Build
        run: npm run build
        working-directory: apps/driver-portal

      - name: Restart app with PM2
        run: pm2 restart aleet-driver-portal || pm2 start npm --name aleet-driver-portal -- start
        working-directory: apps/driver-portal
